name: custom_overlay_popup
description: "A customizable popup that can be called on the widget of your choice."
version: 0.0.9
homepage: https://github.com/SD2B/overlay_popup.git

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:

  